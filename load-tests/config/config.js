// load-tests/config/config.js
export const BASE_URL = __ENV.BASE_URL || 'http://localhost:4000'; // Ajusta según tu configuración

export const endpoints = {
  // Agrega endpoints 
};

export const users = [
  // Agrega usuarios de prueba
];

export const projectData = {

};