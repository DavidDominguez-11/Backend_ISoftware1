POSTGRES_USER=usuario
POSTGRES_PASSWORD="secret"
POSTGRES_DB=test_db
PORT_DB=5432
DB_HOST=postgres

SERVER_PORT_TEST=4000

JWT_SECRET=clave

TOKEN_SIGN="your_jwt_secret_key_here_change_this_in_production"

# For Docker containers (backend connects to postgres service)
DATABASE_URL="postgresql://usuario:secret@postgres:5432/test_db"